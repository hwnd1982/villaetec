{
  "scripts": {
    "postcss": "npx postcss css/*.css --use autoprefixer -d css/postcss/css/",
    "postcss-map": "npx postcss css/*.css --use --map autoprefixer -d css/postcss/css/"
  },
  "dependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.23",
    "postcss-cli": "^10.1.0"
  }
}
